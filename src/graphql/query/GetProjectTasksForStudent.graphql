query GetProjectTasksForStudent($projectId: String!) {
  getProjectTasksForStudent(projectId: $projectId) {
    title
    approxTime
    id
    longDescription
    priority
    shortDescription
  }
}
